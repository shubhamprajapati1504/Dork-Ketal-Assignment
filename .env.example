# Create a local .env file based on this template. Never commit the real key.
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: change the model used for the one-sentence inventory explanation.
GEMINI_MODEL=gemini-3.5-flash
